name: monday_cooks
description: A bunch of cooking together - mostly mondays ...

#publish_to:

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.1
  font_awesome_flutter: ^8.11.0
  sliding_up_panel: ^1.0.2
  firebase_core: ^0.7.0
  firebase_auth: ^0.20.0+1
  cloud_firestore: ^0.16.0
  firebase_storage: ^7.0.0
  modal_progress_hud: ^0.1.3
  flappy_search_bar: ^1.7.2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true
  assets:
    - images/